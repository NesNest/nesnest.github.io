# micomind.github.io
AnnotAI is a web-based platform for managing linguistic annotation projects, designed for tasks such as text classification, named entity recognition (NER), and collaborative validation. It allows users to upload texts, assign roles, perform annotations, validate results, and export structured data in formats like CSV or JSON.
